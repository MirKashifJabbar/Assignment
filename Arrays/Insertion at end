#include <stdio.h>

int main() {
    int arr[]={1,2,4,6};
    int i;
    int n=4;
    int new;
    printf("Enter new no: ");
    scanf("%d", &new);

    arr[n]=new;
    n++;
    for(i=0;i<n;i++){
        printf("%d ",arr[i]);
    }
    return 0;
}
