#include <stdio.h>
void swap(int a,int b);
int main() {
int x=5,y=6;
swap(x,y);
return 0;
}
void swap(int a,int b){
    int temp;
    temp=a;
    a=b;
    b=temp;
    printf("The swapped numbers are:x=%d and y=%d",a,b);
}

